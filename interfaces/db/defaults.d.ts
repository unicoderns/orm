export declare namespace Defaults {
    enum Timestamp {
        'NULL' = 0,
        'ZERO' = 1,
        'CURRENT_TIMESTAMP' = 2,
        'NULL ON UPDATE CURRENT_TIMESTAMP' = 3,
        'CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP' = 4
    }
    enum Binary {
        'FALSE' = 0,
        'TRUE' = 1
    }
}
